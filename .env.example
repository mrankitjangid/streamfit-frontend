# Environment variables for local development
VITE_SOCKET_URL=http://localhost:3000

# Backend Environment Variables
BACKEND_PORT=3000
PORT=3000

# CORS Configuration (if needed)
CORS_ORIGIN=*

# MediaSoup Configuration (if needed)
NUM_WORKERS=1
